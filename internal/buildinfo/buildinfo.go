package buildinfo

const ModuleName = "agentwharf"

// Version is replaced by the release build. Development builds deliberately
// do not pretend to be an installed release and therefore skip update checks.
var Version = "dev"
